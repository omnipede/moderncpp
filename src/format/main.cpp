#include <iostream>
#include <format>

// Format

int main() {
    // format: 포맷 스트링 반환
    // format_to: 결과를 output iterator 에 반환
    // format_to_n: 최대 n 개 문자를 output iterator 에 반환

    // int value = 20;
    // std::cout << format("Hello c++{}", value) << std::endl;
    // string buffer;
    // format_to(std::back_inserter(buffer), "Hello C++{}", 20);

}